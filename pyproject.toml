[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "benchmark-engine"
version = "0.1.0"
description = "A domain-agnostic benchmark engine"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["benchmark_engine*"]
